{
  "name": "lyra-signal",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Ingestion and signal pipeline (Solana RPC, PumpPortal WS, filters, AI copy)",
  "scripts": {
    "smoke": "node scripts/smoke.mjs",
    "dev": "node --env-file=.env --import tsx src/dev-server.ts",
    "start": "node --import tsx src/dev-server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/scoring/algorithm/core.test.ts"
  },
  "dependencies": {
    "tsx": "^4.19.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
