/**
 * GMGN — use only documented/keyed APIs per compliance; otherwise omit in production.
 */
export async function startGmgnWorker(_signal: AbortSignal): Promise<void> {
  await Promise.resolve();
}
